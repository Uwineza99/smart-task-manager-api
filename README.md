# smart-task-manager-api
Smart Task Manager API is a simple backend project using Node.js with Express as backend framework.. It allows creating and listing the users and checking the server health, where the users are stored temporarily.
